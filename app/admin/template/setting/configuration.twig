{% extends "_default/layout.base.twig" %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="/">{% trans "Home" %}</a></li>
		<li>{% trans "Settings" %}</li>
		<li class="active">{% trans "Configuration" %}</li>
	</ol>

	{% if env.sticky_session.message is defined %}
		{{ base.display_flash_message(env.sticky_session.message, 'configuration') }}
	{% endif %}

	<form action="/setting/configuration" method="post" class="form-condensed form-horizontal">
		<div class="panel panel-default">
			<div class="panel-heading">{% trans "Configuration" %}</div>
			<div class="panel-body">
				<div class="form-group">
					<label class="col-xs-3 control-label">{% trans "Archive mailbox" %}</label>
					<div class="col-xs-9">
						<input type="text" name="setting[archive_mailbox]" class="form-control" value="{{ settings.archive_mailbox }}">
					</div>
				</div>

				<div class="form-group">
					<div class="col-xs-9 col-xs-offset-3">
						<button type="submit" class="btn btn-primary">{% trans "Save" %}</button>
					</div>
				</div>
			</div>
		</div>

		<div class="panel panel-default">
			<div class="panel-heading">{% trans "Skins" %}</div>
			<div class="panel-body">
				<div class="form-group">
					<label class="col-xs-3 control-label">{% trans "Email skin" %}</label>
					<div class="col-xs-9">
						<select name="setting[skin_email_id]" class="form-control">
							{% for skin_email in skin_emails %}
								<option value="{{ skin_email.id }}" {% if skin_email.id == setting.skin_email_id %}selected{% endif %}>{{ skin_email.description }}</option>
							{% endfor %}
						</select>
					</div>
				</div>

				<div class="form-group">
					<div class="col-xs-9 col-xs-offset-3">
						<button type="submit" class="btn btn-primary">{% trans "Save" %}</button>
					</div>
				</div>

			</div>
		</div>

		<div class="panel panel-default">
			<div class="panel-heading">{% trans "PDF templates" %}</div>
			<div class="panel-body">
				<div class="form-group">
				    <label class="col-xs-3 control-label">{% trans "New invoice" %}</label>
				    <div class="col-xs-9">
				        <input type="text" name="setting[invoice_pdf_template]" class="form-control" value="{{ settings.invoice_pdf_template }}">
						<span class="help-block">{% trans "If empty, the default 'invoice' is used." %}</span>
				    </div>
				</div>

				<div class="form-group">
					<div class="col-xs-9 col-xs-offset-3">
						<button type="submit" class="btn btn-primary">{% trans "Save" %}</button>
					</div>
				</div>
			</div>
		</div>
	</form>

{% endblock content %}
